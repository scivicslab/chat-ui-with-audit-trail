# chat-ui-with-audit-trail
quarkus.http.port=28030

# ---- ChatSession stage 1 (openai-compat only; no agent loop yet) ----
# Comma-separated OpenAI-compatible server base URLs (vLLM, Ollama, etc.)
chat-ui.servers=http://192.168.5.16:8000
# Default model (leave blank to auto-detect the first available)
chat-ui.default-model=google/gemma-4-26B-A4B-it
