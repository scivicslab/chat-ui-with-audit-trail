<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.scivicslab</groupId>
  <artifactId>chat-ui-with-audit-trail</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <compiler-plugin.version>3.14.0</compiler-plugin.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.version>3.28.3</quarkus.platform.version>
    <surefire-plugin.version>3.5.4</surefire-plugin.version>
    <!-- Actor model + Turing Workflow engine (IIActorSystem / RootIIAR) for the Actors tab -->
    <pojo-actor.version>3.5.0</pojo-actor.version>
    <turing-workflow.version>3.7.0</turing-workflow.version>
    <!-- uber-jar without the -runner suffix, per the project naming rule -->
    <quarkus.package.jar.type>uber-jar</quarkus.package.jar.type>
    <quarkus.package.jar.add-runner-suffix>false</quarkus.package.jar.add-runner-suffix>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- REST + Qute templating for the server-rendered console shell -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-qute</artifactId>
    </dependency>
    <!-- JSON serialization for GET /api/actors -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <!-- POJO-actor: the actor model that backs the actor tree -->
    <dependency>
      <groupId>com.scivicslab</groupId>
      <artifactId>pojo-actor</artifactId>
      <version>${pojo-actor.version}</version>
    </dependency>
    <!-- Turing Workflow: provides IIActorSystem and its auto-created RootIIAR root actor -->
    <dependency>
      <groupId>com.scivicslab</groupId>
      <artifactId>turing-workflow</artifactId>
      <version>${turing-workflow.version}</version>
    </dependency>
    <!-- turing-workflow:3.7.0's own published pom omits this, even though Interpreter (which
         ChatSession extends) uses org.apache.commons.jexl3 directly — declared explicitly here
         so it resolves at runtime, not just compile time. -->
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-jexl3</artifactId>
      <version>3.3</version>
    </dependency>
    <!-- H2-backed complete I/O log (Sessions tab / audit trail). Only the storage primitives are
         used (H2LogStore / DistributedLogStore / SessionStatus), which depend on H2 + org.json only.
         pojo-actor and turing-workflow are excluded so this does NOT pull a second pojo-actor
         version — this project stays on its own; the H2 classes never reference them. -->
    <dependency>
      <groupId>com.scivicslab.turingworkflow.plugins</groupId>
      <artifactId>plugin-log-db</artifactId>
      <version>1.6.0</version>
      <exclusions>
        <exclusion>
          <groupId>com.scivicslab</groupId>
          <artifactId>pojo-actor</artifactId>
        </exclusion>
        <exclusion>
          <groupId>com.scivicslab</groupId>
          <artifactId>turing-workflow</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- Health probe for Kubernetes, per the project standard -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${quarkus.platform.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
              <goal>generate-code-tests</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler-plugin.version}</version>
        <configuration>
          <compilerArgs>
            <arg>-parameters</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire-plugin.version}</version>
        <configuration>
          <systemPropertyVariables>
            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
